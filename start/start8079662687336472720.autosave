Circle c = new Circle(new PVector(0, 0));
PVector gravity = new PVector(0, 1);

void setup() {
    size(400, 400);
    strokeWeight(2);
}

void draw() {
    translate(width / 2, height / 2);
    c.apply(gravity);
    c.show();
}
